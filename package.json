{
  "name": "photo-gallery",
  "version": "1.0.0",
  "private": true,
  "description": "A personal photo gallery with uploads, tags, and persistent metadata.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check script.js && node --check scripts/migrate-to-render.js",
    "migrate:render": "node scripts/migrate-to-render.js",
    "migrate:remote": "node scripts/migrate-to-render.js"
  }
}
